<LugarGeneracionXML Pais="{{ generation_place.country.code }}"
    DepartamentoEstado="{{ generation_place.state.code }}"
    MunicipioCiudad="{{ generation_place.city.code }}"
    Idioma="{{ generation_place.language.code }}" />
